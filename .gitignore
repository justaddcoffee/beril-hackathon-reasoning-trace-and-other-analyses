data/
# Consent table - names + per-person decisions. Lives under data/ (already
# gitignored), but called out explicitly so it's obvious this file must
# never be committed even if the data/ rule changes.
data/consent.csv
*.zip
.ipynb_checkpoints/
__pycache__/
*.pyc
.venv/
.env
.DS_Store
# local Claude Code settings
.claude/
# Working files for the human labeling pass. These contain trace-content
# snippets and per-session usernames; not safe to push before the consent
# filter is applied (Ben opted out, Nik Chia did not reply). Sanitized
# outputs (notes/99_writeup.md, etc.) can be published.
notes/01_inventory.csv
notes/01d_first_prompts.csv
notes/02_outcomes_to_label.csv
